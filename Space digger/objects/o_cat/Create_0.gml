currentcamerapos = (camera_get_view_y(view_camera[0]) + 795) //checking for the bottom of the camera + a bit extra to make it more fair.
playerisdead = 0;
scorekeeper = 0; //This variable is here so the score never drops.
ini_close();